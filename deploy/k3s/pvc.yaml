apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: note-keeper-data
  namespace: note-keeper
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  # k3s default local-path StorageClass
  storageClassName: local-path
