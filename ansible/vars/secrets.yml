---
# Секреты подставляются из переменных окружения (загружены из .env через mise)
# AES-256 key (Base64 encoded). Generate with: EncryptionService.generateKey()
app_encryption_key: "{{ lookup('env', 'NOTE_KEEPER_ENCRYPTION_KEY') }}"
jwt_secret: "{{ lookup('env', 'JWT_SECRET') }}"
